# Dependencies (se instalan dentro de la imagen)
node_modules
npm-debug.log

# Build output (se genera dentro de la imagen)
dist
coverage
*.tsbuildinfo

# Entorno y secretos (NO deben entrar a la imagen)
.env
.env.*
!.env.example

# Git / VCS
.git
.gitignore
.gitattributes

# Docker
Dockerfile
docker-compose*.yml
.dockerignore

# Editor / OS
.vscode
.idea
.DS_Store
Thumbs.db

# Tests y docs no necesarios en runtime
test
**/*.spec.ts
README.md
